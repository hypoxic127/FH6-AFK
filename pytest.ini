[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    hardware: tests requiring Windows hardware (vgamepad, mss, game window)
addopts = -v --tb=short -m "not hardware"
